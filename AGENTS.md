do not run make
do not use git
do not deploy
do not run benchmarks
for any above, ask the user to run them
never fix to code just to pass a specific benchmark. the code should be generic. Do not modify the benchmark.py to patch user specified SQL
prefer a scientific approach. write benchmarks. see results, then we iterate until we have solved the benchmark
tests are only to validate functionality
